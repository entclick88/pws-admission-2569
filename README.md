# 🎓 ระบบรับนักเรียนใหม่ โรงเรียน มอ. วิทยานุสรณ์ สุราษฎร์ธานี ปีการศึกษา 2569

ระบบรับสมัครสอบ ประกาศรายชื่อ และประกาศผลสอบออนไลน์ สร้างตาม
[ประกาศรับสมัครฉบับเต็ม](https://drive.google.com/file/d/110pKpTINDjpZWNp7_QY-79MpTcHAiW5N/view)
ลงวันที่ 3 พฤศจิกายน 2568

## สถาปัตยกรรม

```
GitHub Pages (docs/)  ──┬─→  Cloudinary                      : ไฟล์แนบ (สลิป / ปพ.1)
                        └─→  Cloudflare Worker  ─→  D1        : ใบสมัคร + ประกาศ + ผลสอบ ทั้งหมด
```

ข้อมูลทั้งหมด (ใบสมัคร ประกาศ FAQ รายชื่อ/ที่นั่งสอบ ผลสอบ และ session ผู้ดูแล) เก็บใน **Cloudflare D1**
ที่เดียว อ่านเร็วและรับผู้ใช้พร้อมกันจำนวนมากได้ · ไฟล์แนบ (รูป/PDF) เก็บที่ Cloudinary · ไม่ใช้ Google Sheet

## หน้าเว็บ

| ไฟล์ | สำหรับใคร | ทำอะไร |
|------|-----------|--------|
| [`docs/index.html`](docs/index.html) | ผู้สมัคร / ผู้ปกครอง | ประกาศ · สืบค้นรายบุคคลด้วยเลขบัตรประชาชน · บัตรประจำตัวผู้เข้าสอบ · ผลสอบ · chatbot |
| [`docs/register.html`](docs/register.html) | ผู้สมัคร | กรอกใบสมัคร · อัปโหลดหลักฐาน · แก้ไขใบสมัคร · **ออกใบสมัคร PDF** |
| [`docs/admin.html`](docs/admin.html) | เจ้าหน้าที่ | นำเข้ารายชื่อ/ผลสอบ · เปิด-ปิดการประกาศ · จัดการประกาศและ FAQ · ดาวน์โหลด CSV ใบสมัคร |
| [`docs/config.js`](docs/config.js) | — | ตั้งค่า URL ทั้งหมดที่เดียว |

### ใบสมัคร (`register.html`)

- 4 ประเภทการสมัคร — สอบคัดเลือกเข้า ม.1 / ม.4 และ Pre-Test ช่วงชั้น ป.4-6 / ม.1-3
  ฟอร์มปรับตัวเลือกและเงื่อนไขตามประเภทที่เลือกอัตโนมัติ
- 3 กลุ่มการรับสมัคร — บุคคลทั่วไป 80% · บุตร น้อง หลาน บุคลากร/ศิษย์เก่า ม.อ. 10% · ผู้มีอุปการคุณ 10%
- ตรวจคุณสมบัติอัตโนมัติ — เลขบัตรประชาชนตรวจด้วย checksum จริงทั้งฝั่งเว็บและเซิร์ฟเวอร์,
  ผู้สมัคร ม.4 ต้องมี GPAX ≥ 2.50 และแนบ ปพ.1 : บ + ใบรับรองความประพฤติเป็น PDF ไฟล์เดียว
- 1 เลขบัตร 1 ใบสมัคร · เลขที่ใบสมัครออกอัตโนมัติแยกตามประเภท (`M1-0001` `M4-0001` `PP-0001` `PM-0001`)
  ใช้ตาราง `app_counters` + `UPDATE ... RETURNING` ใน D1 กันเลขซ้ำเมื่อมีผู้สมัครพร้อมกัน
- แก้ไขใบสมัครและพิมพ์ซ้ำได้ด้วยเลขบัตรประชาชน + วันเกิด
- ใบสมัคร PDF ขนาด A4 หน้าเดียว สร้างด้วย html2pdf (ภาษาไทยคมชัด) มีปุ่มพิมพ์เป็นทางสำรอง

### หน้าสืบค้น (`index.html`)

ผู้สมัครกรอกเลขประจำตัวประชาชน 13 หลัก แล้วเห็นข้อมูล**เฉพาะของตนเอง** 4 รอบ

| รอบ (`stage`) | แสดงอะไร |
|---------------|----------|
| `exam_card` | บัตรประจำตัวผู้เข้าสอบ — เลขที่นั่งสอบ ห้องสอบ (พิมพ์ลง A4 ได้) |
| `round1` | ผลสอบรอบแรก (ข้อเขียน) และผลสอบวัดความรู้ |
| `interview` | บัตรประจำตัวสอบสัมภาษณ์ รอบสอง |
| `final` | ผลการคัดเลือกเข้าศึกษาต่อ (ผู้ผ่านเกณฑ์มาตรฐาน) |

แต่ละรอบมีสวิตช์ **เปิด/ปิดการประกาศ** แยกกัน — ตราบใดที่ยังปิด ผู้สมัครเห็นเพียงข้อความแจ้งกำหนดการ
ไม่เห็นข้อมูลรายบุคคล จึงนำเข้าข้อมูลล่วงหน้าได้อย่างปลอดภัยแล้วค่อยเปิดตอนถึงเวลาประกาศจริง

---

## ติดตั้ง

### 1. Cloudinary (ไฟล์แนบ)

1. สมัคร [cloudinary.com](https://cloudinary.com) (ฟรี) → จด **Cloud name**
2. Settings → Upload → Upload presets → Add upload preset
   ตั้ง **Signing Mode = Unsigned** → จดชื่อ preset

### 2. Google OAuth (เข้าสู่ระบบผู้ดูแลด้วย Google)

1. [Google Cloud Console](https://console.cloud.google.com) → สร้างโปรเจกต์ → OAuth consent screen (External, Publish)
2. Credentials → Create Credentials → **OAuth client ID** → Application type **Web application**
3. **Authorized JavaScript origins** ใส่ URL ของ GitHub Pages (เช่น `https://entclick88.github.io`)
   และ `http://localhost:8744` ถ้าจะทดสอบในเครื่อง
4. คัดลอก **Client ID** ไปใส่ทั้งใน `docs/config.js` (`GOOGLE_CLIENT_ID`) และ `worker/wrangler.toml`
   (`GOOGLE_CLIENT_ID`) — ต้องเป็นค่าเดียวกัน

> ใครเข้าระบบผู้ดูแลได้บ้าง คุมที่ `worker/wrangler.toml` — `ALLOWED_DOMAINS` (ทุกคนในโดเมน
> เช่น `psuwitsurat.ac.th`) และ `ADMIN_EMAILS` (อีเมลเฉพาะราย) บัญชีที่ login ครั้งแรกจะถูกบันทึกลงตาราง `users` อัตโนมัติ

### 3. Cloudflare Worker + D1 (เก็บข้อมูลทั้งหมด)

```bash
cd worker
npx wrangler d1 create pws-admission          # นำ database_id ที่ได้ไปใส่ใน wrangler.toml
npx wrangler d1 execute pws-admission --remote --file=schema.sql
npx wrangler deploy                            # ได้ URL https://pws-admission-api.<ชื่อ>.workers.dev
```

แก้ `worker/wrangler.toml` ก่อน deploy: ใส่ `database_id`, `GOOGLE_CLIENT_ID`, `ADMIN_EMAILS`, `ALLOWED_DOMAINS`

`schema.sql` สร้างตารางพร้อมข้อมูลตั้งต้นให้เลย — `applications` + `app_counters` (ใบสมัคร),
ผู้ดูแล/`sessions`, ประกาศ 3 รายการ, รอบการประกาศ 4 รอบ, และ **FAQ 25 ชุดที่แตกจากประกาศฉบับเต็ม**

### 4. เชื่อมทุกอย่างเข้าด้วยกัน

แก้ [`docs/config.js`](docs/config.js) แล้ว push ขึ้น GitHub

```js
WORKER_URL:        "https://pws-admission-api.xxx.workers.dev",
GOOGLE_CLIENT_ID:  "xxxx.apps.googleusercontent.com",
CLOUDINARY_CLOUD:  "your-cloud-name",
CLOUDINARY_PRESET: "your-unsigned-preset",
```

เปิด GitHub Pages โดย serve จากโฟลเดอร์ `docs/`

## การเข้าสู่ระบบผู้ดูแล (admin.html)

- เจ้าหน้าที่กดปุ่ม **"ลงชื่อเข้าใช้ด้วย Google"** เลือกบัญชีโรงเรียน — ไม่มีรหัสผ่านให้จำ
- ติ๊ก **"จดจำการเข้าสู่ระบบไว้ 30 วัน"** (ค่าเริ่มต้น) เพื่อให้เข้าครั้งต่อไปโดยไม่ต้อง login ซ้ำ
  (session token เก็บใน `localStorage` ของเครื่องนั้น) หากไม่ติ๊ก session จะหมดเมื่อปิดเบราว์เซอร์ (12 ชม.)
- ดึงใบสมัครจาก Google Sheet ผ่าน Worker เป็นตัวกลาง (`/api/admin/applications`) — เบราว์เซอร์ไม่ต้องรู้รหัส Sheet
- ผู้สมัคร/ผู้ปกครองยัง**ไม่ต้อง login** สืบค้นด้วยเลขบัตรประชาชนเหมือนเดิม

---

## การใช้งานประจำวันของเจ้าหน้าที่

เข้า `admin.html` → **ลงชื่อเข้าใช้ด้วย Google** (บัญชีในโดเมนที่กำหนดหรืออีเมลใน `ADMIN_EMAILS`)

**ดูใบสมัคร / ดาวน์โหลด CSV** — แท็บ "ใบสมัคร (CSV)" ดึงใบสมัครทั้งหมดจาก D1 มาแสดงและดาวน์โหลดเป็น CSV
(มี BOM เปิดใน Excel ภาษาไทยไม่เพี้ยน)

**นำเข้ารายชื่อ / ผลสอบ** — คัดลอกตารางจาก Excel หรือ Google Sheets มาวางได้เลย
(รองรับทั้งข้อมูลคั่นด้วย Tab และ CSV) ต้องมีคอลัมน์ `citizen_id` เสมอ ระบบจะข้ามแถวที่เลขไม่ครบ 13 หลัก
และมีปุ่มตรวจสอบข้อมูลก่อนนำเข้าจริง

คอลัมน์ที่รองรับแต่ละรอบ

| รอบ | คอลัมน์ |
|-----|---------|
| `exam_card` | `citizen_id` `full_name` `app_no` `exam_type` `seat_no` `room` `report_room` `exam_date` `time_slot` `note` |
| `round1` | `citizen_id` `full_name` `app_no` `exam_type` `status` `score` `rank_no` `note` |
| `interview` | `citizen_id` `full_name` `app_no` `exam_type` `room` `report_room` `exam_date` `time_slot` `note` |
| `final` | `citizen_id` `full_name` `app_no` `exam_type` `status` `note` |

นำเข้าซ้ำได้ — ระบบอัปเดตทับรายคน (จับคู่ด้วย `citizen_id`) และเพิ่มคนใหม่ให้อัตโนมัติ
หรือติ๊ก "ลบข้อมูลเดิมก่อนนำเข้า" ถ้าต้องการแทนที่ทั้งชุด

**เปิดประกาศ** — เมื่อถึงเวลาประกาศตามปฏิทิน เข้าแท็บ "เปิด/ปิดการประกาศ" แล้วติ๊กเปิดรอบนั้น

---

## ปฏิทินดำเนินงาน

| วันที่ | รายการ |
|--------|--------|
| 3 พ.ย. 2568 – 5 ม.ค. 2569 | รับสมัครสอบคัดเลือกและสอบวัดความรู้ |
| 15 ม.ค. 2569 | ประกาศรายชื่อผู้มีสิทธิ์สอบ เลขที่นั่งสอบ และพิมพ์บัตรประจำตัวผู้เข้าสอบ |
| 25 ม.ค. 2569 | สอบคัดเลือกรอบแรก (ข้อเขียน) และสอบวัดความรู้ |
| 3 ก.พ. 2569 | ประกาศผลสอบรอบแรกและผลสอบวัดความรู้ |
| 11 ก.พ. 2569 | ประกาศรายชื่อผู้มีสิทธิ์สอบรอบสอง |
| 15 ก.พ. 2569 | สอบสัมภาษณ์นักเรียนพร้อมผู้ปกครอง |
| 19 ก.พ. 2569 | ประกาศรายชื่อผู้ผ่านเกณฑ์มาตรฐาน |
| 19 – 24 ก.พ. 2569 | ยืนยันสิทธิ์เข้าศึกษาต่อ |
| 5 มี.ค. 2569 | ประกาศรายชื่อผู้ยืนยันสิทธิ์ |
| 15 มี.ค. 2569 | มอบตัว - ปฐมนิเทศ และพิธีมอบเกียรติบัตร Pre-Test |
| 18 พ.ค. 2569 | เปิดภาคเรียนที่ 1 ปีการศึกษา 2569 |

## หมายเหตุการพัฒนา

- แก้ Worker แล้วรัน `npx wrangler deploy` ใหม่ · แก้ไฟล์ใน `docs/` แค่ push ขึ้น GitHub
- ใบสมัครเก็บใน D1 ตาราง `applications` — ฟิลด์ทั้งหมดเก็บเป็น JSON ในคอลัมน์ `data` (คีย์ camelCase
  ตรงกับ `EXAM_CSV_HEADERS` ใน `admin.html`) · รับเฉพาะไฟล์แนบที่เป็น URL ของ Cloudinary
- ความสูงของใบสมัคร (`#appSlip` ใน `register.html`) ต้องไม่เกิน ~1.42 เท่าของความกว้าง
  ไม่งั้น PDF จะล้นไปหน้าที่ 2

## ติดต่อ

งานรับนักเรียนใหม่ ฝ่ายนโยบายและบริหารการศึกษา · ☎️ 08 4557 9229 กด 5
🆔 LINE : psuwitsurat · 📩 pws-newgen@psuwitsurat.ac.th
🌐 https://sites.google.com/psuwitsurat.ac.th/pws-newgen
